import 'package:flutter/material.dart';
class hahapage extends StatelessWidget {
  const hahapage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
